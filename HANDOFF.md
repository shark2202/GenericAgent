# HANDOFF — hermes self-evolution (v1 feature-complete; v1.5 planned)

> Fresh-agent handoff. Authoritative design lives in the artifacts referenced
> below — **read those, not this doc, for spec/decisions.** This file only
> captures state + how to continue. Two prior threads are folded in: (1) the
> hermes v1 implementation (post-task skill distillation), (2) this session's
> deep-research-derived v1.5 recommendations.

## What this is

Enhancing `ga.py` (`GenericAgentHandler`) into a self-evolving agent "hermes":
a post-task skill-distillation loop where the agent crystallizes reusable
Skills (`.agents/skills/<name>/SKILL.md` with provenance frontmatter) after
complex tasks, via a controlled `skill_manage` tool, with circuit breakers and
a human-in-the-loop Brief checkpoint.

**v1 is feature-complete** (code written, unit-tested, ruff-clean on new code).
Two open threads: (a) **Step 5b integration tests** (v1 DoD gap — needs a
deps-complete env), and (b) **v1.5** (not started — this session produced the
plan; see `hermes/NOTES_v1.5_recommendations.md`).

## What this session did

1. **Deep research on the Chinese "Agent Skill" ecosystem** (user invoked
   `/deep-research` with 6 URLs). The Gemini harness (`scripts/research.py`)
   could NOT run — `GEMINI_API_KEY` is unset and no `.env` exists at
   `C:\Users\Administrator\.claude\skills\deep-research`. Ran the fan-out
   directly via `WebFetch` instead.
   - **5 GitHub repos fetched successfully** (darwin-skill, nuwa-skill,
     Yeadon8888/cangjie-skill, kangarooking/cangjie-skill, X-growth-skills).
   - **WeChat article inaccessible** (`mp.weixin.qq.com/s/9ZcZRv_AYiinhh52WGAPCw`
     — verification wall). Content unknown.
   - **WebSearch non-functional** in this environment (US-only, returning empty);
     Wayback Machine blocked by the harness. So darwin-skill's citation of a
     "Microsoft Research SkillLens / SkillOpt" paper is **unverified** — treat
     as the author's self-claim until checked in a web-capable env.
2. **Cross-referenced the ecosystem's patterns against the actual hermes v1
   code** (spec + `plugins/skill_evolution.py` + `ga.py` `do_skill_manage` +
   5 new `skill_loader` fns) and produced **9 prioritized v1.5 recommendations
   (R1–R9)** with file:line refs. Saved to
   `hermes/NOTES_v1.5_recommendations.md` (the primary new artifact).

## Authoritative artifacts (READ FIRST, do not re-derive)

| Artifact | Purpose |
|---|---|
| `hermes/NOTES_v1.5_recommendations.md` | **THIS SESSION'S OUTPUT** — v1.5 recommendations R1–R9, each with hermes-gap (code refs) + fix + insertion point; prioritized roadmap; ecosystem pattern mapping. Start here for v1.5. |
| `hermes/workflows/post-task-skill-distillation.md` | FINAL v1 buildable spec — Python interface signatures, hook contracts, decisions D1–D8, 验收 T1–T10, v1不做 boundary. |
| `hermes/NOTES.md` | Reference repos' mechanisms (hermes-agent / jiuwenswarm), existing GA assets, decision rationale. |
| `AGENTS.md` | Repo build/verify commands (ruff E/F/W/I/UP/B len120 E501 ignored; `uv pip install -e ".[ui]"`; `pytest tests/`). |

## Ecosystem map (condensed — full detail in the research conversation / NOTES)

A mythology-themed three-stage skill lifecycle, all on the SKILL.md /
[agentskills.io](https://agentskills.io) format: **女娲/nuwa** (create — distills
*people*, how they think) → **仓颉/cangjie** (distill — ⚠️ naming collision:
Yeadon8888's distills *people* deeper "think like not talk like"; kangarooking's
distills *books* via RIA-TV++) → **达尔文/darwin** (evolve — autonomous
optimization loop, ratchet, independent sub-agent scoring, 8 anti-patterns).
`X-growth-skills` is an applied pack *generated by* kangarooking's cangjie.
hermes v1's `distill()` is the nuwa/cangjie *distill* stage; the dormant
`fitness` field + `_auto_patch_counts` + `.prev` are the seed of darwin's
*evolve* stage. **v1.5 = the bridge from one-shot distill to light evolution.**

## v1.5 recommendations summary (full detail in `hermes/NOTES_v1.5_recommendations.md`)

| Tier | Items | Effort |
|---|---|---|
| **0 — correctness fixes (do first)** | R6 breaker-reset bug · R7 dead-counter cleanup · R8 verify §7 fitness-log write · R9 validate loadability check | low |
| **1 — v1.5 core** | R1 wire negative-signal hooks (planned v1.5) · R4 honesty/boundary declaration | low–med |
| **2 — quality** | R2 independent scorer/gatekeeper · R3 per-skill `test-prompts.json` + empirical scoring | med |
| **v2** | R5 ratchet (revert regressing patches) · full 9-dim rubric · INDEX graph · embeddings · scheduled reflection | high |

**Most important finding:** **R6** — `_auto_patch_counts`
(`plugins/skill_evolution.py:37`) increments on successful auto-patch (`:104`)
and trips the breaker at ≥5 (`:88`), but is **never reset** (the `:36` comment
claims "前台修正时重置" but no reset code exists). After 5 auto-patches a skill
is permanently breaker-locked until process restart. Concrete v1.5 correctness
bug, not just a design nicety.

**What hermes already does right** (confirmed against the ecosystem): avoids
darwin's `git reset --hard` anti-pattern (uses `.prev`/`.bak` file snapshots +
`revert_skill_prev`, no git); follows darwin principle #1 single-editable-asset
(`distill()` emits ≤1 op); Brief flush correctly batched at `turn%10`/task-end
(`ga.py:818`).

## Remaining work — pick one to continue

1. **Implement Tier 0** (R6/R7/R8/R9) — low-effort, mostly confined to
   `plugins/skill_evolution.py` + `ga.py`/`skill_loader.py`. These are real
   bugs/gaps, not speculative features. Start here if continuing v1.5.
2. **Start a comet change for v1.5** (`openspec/changes/hermes-v1.5-skill-evolution`)
   — the proper path per the repo's comet workflow if implementing Tier 1–2.
   The recommendations doc is ready to become its proposal/design.
3. **Verify the SkillLens/SkillOpt claim** — fetch darwin-skill's raw README
   (via `gh api`) in a web-capable env to see which MS Research papers it cites;
   would substantiate R2's "independent scorer" rationale. Also retry the
   WeChat article (open in a browser, paste text back).
4. **Step 5b — v1 integration tests (still pending, see below).**

## Step 5b — integration tests T1/T3/T8 (v1 DoD gap, still open)

**Why blocked:** the Linux sandbox at `/mnt/d/GenericAgent` cannot `import ga.py`
(needs `pywebview` + Windows-heavy deps); repo `.venv` is Windows-only. Needs a
**deps-complete env** (`uv pip install -e ".[ui]"` on Windows, or a Linux box
with pywebview).

**What to do:** add `tests/test_skill_evolution_integration.py`:
- **T1 (trigger):** monkeypatch LLM `client.chat` → `{"action":"create",...}`;
  drive `agent_after` ctx with `current_turn>=6` + `GA_SKILL_EVOLUTION_ENABLED=1`;
  assert a Skill dir is created under a `tmp_path`-rooted `.agents/skills/`
  with correct provenance frontmatter.
- **T3 (patch end-to-end):** seed an existing `author=agent, evolvable=true`
  skill; LLM returns `{"action":"patch",...}`; assert `.prev` created, content
  updated, `validate_skill` passes, cache reset + L1 sync fired.
- **T8 (Brief fields):** assert `_pending_briefs` entry shape matches
  `build_brief` output (action/name/reason/path + `[approve|edit|revert]`).
- Assert the **read-only boundary**: `author=user` or `evolvable=false` skill
  → `patch` returns suggestion, no file change.
- Assert the **circuit breaker**: after `MAX_AUTO_PATCH_PER_SKILL`(=5) successful
  auto-patches, the 6th produces a Brief, not a file write.
- Mock `client.chat` (handler's `do_skill_manage` is gen-style returning
  `StepOutcome`; `_drain` in the plugin exhausts it). Run
  `pytest tests/test_skill_evolution_integration.py` + rerun the evolution trio
  (`test_skill_evolution.py` + `test_skill_evolution_plugin.py`, 38 tests) to
  confirm no regressions.

**Hook contract reference** (from `agent_loop.py`, lines 22/24/49/57/58/67/103/106
— `_hook(event, locals())`):
- `agent_after` ctx: `handler`, `client`, `exit_reason` (dict with `result` ∈
  `CURRENT_TASK_DONE`/`EXITED`/`MAX_TURNS_EXCEEDED`).
- `tool_after` ctx: `self`, `tool_name`, `args`, `ret` (StepOutcome).
- `turn_after` ctx: `turn`, `next_prompt`, `exit_reason`.

## hermes v1 implementation state (condensed — full detail in git diff + spec)

v1 touches: `skill_loader.py` (+5 public fns: `parse_provenance`/`validate_skill`/
`retrieve_skill_by_desc`/`backup_skill_prev`/`revert_skill_prev`; `.prev` rolling
snapshot is separate from existing `.bak` one-shot — both coexist),
`ga.py` (`do_skill_manage` + `_validate_skill_content`/`_set_frontmatter_flag`/
`_build_skill_brief` helpers; `__init__` +`_pending_briefs`/`_evolution_signal`;
`turn_end_callback` flush; `do_start_long_term_update` spawns
`distill(signal='manual')`), `agent_loop.py` (+1 line `handler.client = client`
at line 49), both `assets/tools_schema*.json` (+`skill_manage`),
`assets/sys_prompt.txt`, `memory/memory_management_sop.md`; **new**
`plugins/skill_evolution.py` (hooks + `distill`/`build_brief`/D8 circuit breaker,
auto-loaded by `plugins.hooks.discover_and_load()` from `agentmain.py:12`),
`tests/test_skill_evolution.py` (17), `tests/test_skill_evolution_plugin.py` (21).

`grep -n "def do_skill_manage\|def distill" ga.py plugins/skill_evolution.py`
locates the entrypoints. **Verification:** ruff clean on new code; 38 tests pass
(stable ×2). `test_skill_loader_l1.py` has 2 **pre-existing** mtime-granularity
flakiness failures (proven via `git stash`; not a hermes regression — no mtime/cache
code touched).

⚠️ `git status` shows a **large pre-existing uncommitted churn** (frontends/ga-tui
rust deletion, ga_rust, go-agents, openspec archive, ga_cli, mcp_client, etc.)
that is **NOT the hermes feature**. This session's only new artifact is
`hermes/NOTES_v1.5_recommendations.md`. If committing, commit only the hermes
files listed above.

## Active comet changes (phase-guard awareness)

5 active (non-archive) comet changes exist, all **unrelated to hermes**:
`skill-lazy-load-mtime-cache` (build), `fix-skill-loader-home-windows`
(build/hotfix), `add-first-class-subagents` (build), `add-selfextract-installer`
(**open** — source-code writes forbidden in this phase), `add-llm-slash-cmd`
(archive). **hermes is NOT yet a comet change** (the prior handoff listed
`comet-verify` as optional). Writing a hermes design note (markdown, not
source) is within phase rules; writing hermes *source* while
`add-selfextract-installer` is in `open` phase is not — either start a proper
hermes comet change or wait. Check phase with `comet-state check <name> <phase>`
before any source write.

## Suggested skills

Invoke at the start of the next session, matched to the chosen next step:

1. **`using-superpowers`** — invoke first; establishes how to find/use skills.
2. **`systematic-debugging`** — for R6 (the breaker-reset bug): root-cause before
   proposing the fix. Also if Step 5b integration tests fail.
3. **`tdd`** (or `test-driven-development`) — for writing Step 5b's T1/T3/T8
   integration tests test-first, and for tests around the Tier 0 fixes.
4. **`verification-before-completion`** — before claiming any Tier 0 fix or
   Step 5b done: run the actual pytest in the deps-complete env, confirm green.
5. **`brainstorming`** — if starting v1.5 as a comet change: design-phase
   refinement of the R1–R9 plan before writing a Design Doc.
6. **`writing-plans`** + **`executing-plans`** / **`subagent-driven-development`**
   — if implementing v1.5 Tier 1–2 as a comet `build`-phase change.
7. **`comet-open`** — if starting the `hermes-v1.5-skill-evolution` comet change
   (open phase: proposal/design/tasks).
8. **`finishing-a-development-branch`** — once v1 (Step 5b) or v1.5 Tier 0
   passes and is verified: decide merge/PR/cleanup. Mind the unrelated
   uncommitted churn — commit only hermes files.

## Environment notes

- Linux sandbox uses `/usr/bin/python3` (3.12) + temp venv at `/tmp/gatestenv`
  (pytest 9.1.1, ruff 0.15.21) — but **cannot import `ga.py`** (Windows deps).
  Run unit tests there: `/tmp/gatestenv/bin/pytest`, `/tmp/gatestenv/bin/ruff`.
- Windows repo path: `D:\GenericAgent`. Bash mount: `/mnt/d/GenericAgent`.
- **Do NOT run whole-file `ruff format` on `skill_loader.py`/`ga.py`** —
  single→double quote normalization is a known repo-wide future task (AGENTS.md);
  it would create massive unrelated diffs. Only format new files.
- `ruff check` emits a deprecation warning about top-level `select`/`ignore`
  (should move to `[lint]` in `pyproject.toml`) — pre-existing, ignore.
- `/deep-research` Gemini harness is **unconfigured** (no `GEMINI_API_KEY`, no
  `.env` at `C:\Users\Administrator\.claude\skills\deep-research`). To use it
  later, drop a key into that `.env`.

## Quick orientation commands

```bash
cd /mnt/d/GenericAgent                      # (or D:\GenericAgent on Windows)
cat hermes/NOTES_v1.5_recommendations.md    # THIS SESSION's v1.5 plan (R1-R9)
cat hermes/workflows/post-task-skill-distillation.md   # the v1 spec
git diff --stat agent_loop.py ga.py skill_loader.py plugins/skill_evolution.py
grep -n "def do_skill_manage\|def distill\|def _apply_op\|MAX_AUTO_PATCH" \
  ga.py plugins/skill_evolution.py skill_loader.py
/tmp/gatestenv/bin/pytest tests/test_skill_evolution.py tests/test_skill_evolution_plugin.py -q
```

## hermes-isolated-skill-scorer 交接摘要

**Change:** `openspec/changes/hermes-isolated-skill-scorer/`
**Design Doc:** `docs/superpowers/specs/2026-07-19-hermes-isolated-skill-scorer-design.md`
**Plan:** `docs/superpowers/plans/2026-07-19-hermes-isolated-skill-scorer.md`

**收敛点:** `plugins/skill_evolution.py:distill()` 的 `_parse_op` 与 `_apply_op` 之间插
`Scorer.score(op, skill_md, history, catalog)` 闸门。pass+≥阈值 → `_apply_op`;否则
Brief "rejected-by-scorer" 不落盘。

**双实现:** `SubagentScorer`(真隔离,经 `handler._subagent_mgr.run_single`)+
`InProcessScorer`(同进程二次 LLM 兜底)。gate `GA_SKILL_SCORER` + `_subagent_mgr` present 决定。
subagents 未合并到 dev → `_subagent_mgr=None` → 缺省走 inprocess(本 change 可独立落地)。

**R6 修复:** `_apply_op` patch 成功时,foreground 修正或 scored-pass background →
`reset_auto_patch_count(name)` 清零(OQ3 决议);打分 off → 原 v1 累加(legacy)。

**测试:** 单测(test_skill_scoring.py)覆盖 InProcessScorer/SubagentScorer mock/gate 路由/
闸门/独立性/R6;集成测(test_skill_scoring_integration.py)6.7 标 skipif 待 subagents 合并,
6.8 降级链当前 dev 可跑。既有 ~64 测试不破(补 `GA_SKILL_SCORER=off` env 走 legacy 分支)。

**回滚:** `GA_SKILL_SCORER=off` 或关 `GA_SKILL_EVOLUTION_ENABLED`。R6 修复不可回退
(行为修复,回退到"永不复位"bug 态不可接受)。
