# ga-core-cli

Rust single-binary Core + Supervisor CLI contract: session/project state, SQLite, Python GA sidecar runner+IPC, tool-call event timeline, cross-restart persistence, cross-platform; plus stable CLI (galley-style commands, origin triples, JSON schemas, exit codes, approvals/allowlist/YOLO)
